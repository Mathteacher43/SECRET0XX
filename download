/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import React, { useState } from "react";
import { ShieldCheck, FileText, Image as ImageIcon } from "lucide-react";
import { TextAnalyzer } from "./components/TextAnalyzer";
import { ImageAnalyzer } from "./components/ImageAnalyzer";
import { FactCheckGuide } from "./components/FactCheckGuide";
import { HistoryFeed } from "./components/HistoryFeed";

export default function App() {
  const [activeTab, setActiveTab] = useState<"text" | "image">("text");

  return (
    <div className="flex h-screen w-full bg-slate-50 text-slate-900 font-sans overflow-hidden">
      <aside className="w-60 bg-slate-900 text-white flex-col flex-shrink-0 shadow-2xl hidden md:flex">
        <div className="p-6 flex items-center gap-3 border-b border-slate-800">
          <div className="w-8 h-8 bg-indigo-500 rounded flex items-center justify-center font-bold text-white shadow-lg">TL</div>
          <span className="text-xl font-bold tracking-tight">트루렌즈 (TruthLens)</span>
        </div>
        <nav className="flex-1 p-4 space-y-1">
          <div className="text-[10px] uppercase tracking-wider text-slate-500 font-semibold mb-2 px-2">검증 시스템</div>
          <button 
            onClick={() => setActiveTab("text")}
            className={`w-full flex items-center gap-3 px-3 py-2 rounded font-medium transition-colors ${activeTab === 'text' ? 'bg-slate-800 text-indigo-400' : 'text-slate-400 hover:bg-slate-800'}`}
          >
            <FileText className="w-4 h-4" />
            텍스트 및 URL 검증
          </button>
          <button 
            onClick={() => setActiveTab("image")}
            className={`w-full flex items-center gap-3 px-3 py-2 rounded font-medium transition-colors ${activeTab === 'image' ? 'bg-slate-800 text-indigo-400' : 'text-slate-400 hover:bg-slate-800'}`}
          >
            <ImageIcon className="w-4 h-4" />
            이미지 조작 분석
          </button>
        </nav>
        <div className="p-4 border-t border-slate-800">
          <div className="bg-slate-800/50 rounded-lg p-3 text-[11px]">
            <p className="text-slate-400 mb-2 underline">시스템 상태</p>
            <div className="flex items-center gap-2 text-emerald-400">
              <span className="w-2 h-2 rounded-full bg-emerald-500"></span> 교차검증 DB 활성
            </div>
          </div>
        </div>
      </aside>

      <main className="flex-1 flex flex-col overflow-hidden">
        <header className="h-14 bg-white border-b border-slate-200 flex items-center justify-between px-6 shadow-sm z-10 md:hidden">
            <div className="flex items-center gap-2">
              <div className="w-6 h-6 bg-indigo-500 rounded flex items-center justify-center text-[10px] font-bold text-white shadow-lg">TL</div>
              <h1 className="text-sm font-bold text-slate-800">트루렌즈</h1>
            </div>
            <div className="flex bg-slate-100 p-0.5 rounded border border-slate-200">
               <button onClick={() => setActiveTab('text')} className={`px-2 py-1 text-[10px] font-bold rounded ${activeTab === 'text' ? 'bg-white shadow' : 'text-slate-500'}`}>텍스트</button>
               <button onClick={() => setActiveTab('image')} className={`px-2 py-1 text-[10px] font-bold rounded ${activeTab === 'image' ? 'bg-white shadow' : 'text-slate-500'}`}>이미지</button>
            </div>
        </header>

        <header className="h-14 bg-white border-b border-slate-200 items-center justify-between px-6 shadow-sm z-10 hidden md:flex">
          <div className="flex items-center gap-2">
            <h1 className="text-lg font-bold text-slate-800">진위 검증 대시보드</h1>
            <span className="bg-slate-100 text-slate-500 text-[10px] px-2 py-0.5 rounded border border-slate-200">v1.2.4</span>
          </div>
        </header>

        <div className="flex-1 p-4 md:p-6 overflow-y-auto">
          <div className="grid lg:grid-cols-12 gap-6 max-w-7xl mx-auto h-full">
            <div className="lg:col-span-8 flex flex-col gap-6">
              <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-5 md:p-6 flex flex-col">
                <div className="mb-4">
                  <h2 className="text-lg font-bold flex items-center gap-2 text-slate-800">
                    <ShieldCheck className="w-5 h-5 text-indigo-500" />
                    {activeTab === "text" ? "정보 및 뉴스 검증" : "이미지 조작 및 생성 탐지"}
                  </h2>
                  <p className="text-xs text-slate-500 mt-1">
                    {activeTab === "text" 
                      ? "검증이 필요한 뉴스 기사, SNS 게시글 또는 URL을 입력하여 팩트체크 데이터베이스와 대조합니다." 
                      : "이미지를 업로드하여 메타데이터(EXIF)를 분석하고, AI 생성 여부 및 픽셀 조작 흔적을 탐지합니다."}
                  </p>
                </div>
                {activeTab === "text" ? <TextAnalyzer /> : <ImageAnalyzer />}
              </div>
            </div>

            <div className="lg:col-span-4 flex flex-col gap-6">
              <FactCheckGuide />
              <HistoryFeed />
            </div>
            
          </div>
        </div>
      </main>
    </div>
  );
}
